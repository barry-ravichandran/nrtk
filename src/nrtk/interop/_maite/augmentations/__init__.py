"""Private API for MAITE augmentation wrappers.

Intentionally empty. Every wrapper in this package needs the ``maite`` extra, so
re-exporting one here would be an unguarded path around the guard in
:mod:`nrtk.interop`, which is where these classes are published from.
"""
