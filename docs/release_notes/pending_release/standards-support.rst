* Added the ability to advertise ``maite`` components in ``pyproject.toml``.

* Added documentation for supported architectures and minimum hardware requirements

* Added ``.gitlab-ci-nightly.yml`` to support CI for mature releases.
