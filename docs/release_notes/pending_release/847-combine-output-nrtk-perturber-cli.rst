* Added new option ``--combine_output`` to ``nrtk-perturber-cli`` to allow for augmented datasets to be
  flattened into one dataset.

* Updated ``dataset_to_coco`` to work when the length of ``dataset`` is a multiple of the length of ``img_filenames``.

* Updated ``test_dataset_to_coco`` to account for the new functionality.
