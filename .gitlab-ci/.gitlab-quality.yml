###############################################################################
# Stage -- Quality Assurance
###############################################################################

.quality-setup:
  extends: .tox-setup
  stage: quality
  before_script:
    - !reference [.tox-setup, before_script]

precommit:
  extends: .quality-setup
  script:
    - tox -e precommit
