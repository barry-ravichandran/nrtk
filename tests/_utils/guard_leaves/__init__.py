"""Leaf modules used to exercise ``nrtk._guard`` without touching real perturbers.

Deliberately empty: the guard is supposed to point at leaves, so a package that
re-exported them would defeat the thing under test.
"""
